[build]
builder = "RAILPACK"

[deploy]
preDeployCommand = "python manage.py migrate"
startCommand = "daphne -b 0.0.0.0 -p ${PORT:-8000} hotel_pos_backend.asgi:application"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
